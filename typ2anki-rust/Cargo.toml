[package]
name = "typ2anki"
version = "0.1.0"
edition = "2024"

[dependencies]
base64 = "0.22.1"
md5 = "0.8.0"
regex = "1.12.2"
reqwest = { version = "0.12.24", features = ["blocking", "json"] }
serde_json = "1.0.145"

# Added for config parsing and runtime helpers
clap = { version = "4", features = ["derive"] }
glob = "0.3"
html-escape = "0.2"
once_cell = "1.20"
toml = "0.7"
walkdir = "2.5.0"
